import React from 'react';
import styled from 'styled-components'

const Wrap = styled.div`

`;

/* 
    1. Admin Auth level 조건 처리
    2. Input 작업
    3. Post API 연결 
    4. Styling? 적당히 
 */
const Admin = () => {
    // auth level check
    // auth level X -> Routing Main Page 

    return (
        <Wrap>
                {/* input, Need Tech Id ⓐ dummy data ⓑ api  */}
                {/* Title, Subtite, Contents */}
        </Wrap>
    );
};

export default Admin;